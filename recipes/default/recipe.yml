name: Varbase Reports - Default
description: A recipe to manage default installed modules, configs and permissions.
type: install
config:
  actions:
    user.role.content_admin:
      grantPermissions:
        - 'access varbase content reports'
    user.role.site_admin:
      grantPermissions:
        - 'access varbase content reports'
